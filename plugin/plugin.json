{
  "name": "roslyn-lsp-hook",
  "description": "C# and shell hooks for GitHub Copilot CLI: diff-aware Roslyn formatting, ast-grep outline context, and a PowerShell pipeline guard that redirects file/content searches to fd and the bundled fff MCP server.",
  "version": "0.1.0",
  "author": {
    "name": "CSharpLintHook"
  },
  "license": "MIT",
  "keywords": ["csharp", "dotnet", "roslyn", "formatting", "lint", "hooks", "ast-grep", "powershell", "fd", "fff", "mcp"],
  "skills": ["skills/"],
  "hooks": "hooks.json",
  "mcpServers": {
    "fff": {
      "type": "stdio",
      "command": "${PLUGIN_ROOT}/fff-mcp",
      "args": []
    }
  }
}
